import { Platform } from 'react-native'
import { createStyleSheet } from '../../../../../features/base/styles';
import { SynziColor, AppColor } from '../../../Color';

export default createStyleSheet({
  charactercounter: {
    marginLeft: 'auto'
  },
  charactercounter75: {
    color: '#ffdc00'
  },
  charactercounterOverMax: {
    color: '#ff4136'
  }
});
